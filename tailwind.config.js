/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{html,js}", // these all the files which ends with these extension used by the tailwind classes or read only these files while compiling.
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}

